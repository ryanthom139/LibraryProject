#include "stdbool.h"

double dataFunction( double x, double y, int choice );
bool indicator( Node *node, double tolerance, int choice );
void addChildren(Node *node, int choice, int tolerance);
void monitorChild (int count);
